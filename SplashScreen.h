#pragma once

#include "LVGL_Driver.h"

// Показать заставку с логотипом Opel
// duration_ms - время показа в миллисекундах (по умолчанию 1500мс = 1.5 секунды)
void SplashScreen_Show(uint32_t duration_ms = 1500);
